spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huseyin-bacanak/properties
server:
  port: 8888

audit:
  config:
    source: cloud